{
  "name": "api",
  "type": "module",
  "scripts": {
    "code:check": "eslint .",
    "start": "node index.js",
    "setup:layers": "cd lambda/layers && npm install && cd ../../",
    "setup:functions": "cd lambda/functions && npm install && cd ../../",
    "setup:http": "cd http && npm install && cd ../",
    "setup": "npm run step:layers && npm run step:functions && npm run setup:http"
  },
  "devDependencies": {
    "eslint": "^8.14.0",
    "eslint-config-standard": "^17.0.0",
    "eslint-plugin-import": "^2.26.0",
    "eslint-plugin-n": "^15.2.0",
    "eslint-plugin-promise": "^6.0.0"
  }
}
